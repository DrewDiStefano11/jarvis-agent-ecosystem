[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "jarvis-completion-validator"
version = "0.1.0"
description = "Isolated Result Validation, Review, and Completion-Gate Prototype"
readme = "README.md"
requires-python = ">=3.11, <3.13"
dependencies = []

[project.scripts]
jarvis-completion-validator = "jarvis_completion_validator.cli:main"
